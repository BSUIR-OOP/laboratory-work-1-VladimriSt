package forms.pack;

public class Circle extends Ellipse {

	public Circle(boolean visible, String color, int lineWidth, Point centre, int horizRadius, int verticRadius) {
		super(visible, color, lineWidth, centre, horizRadius, horizRadius);
	}

}
